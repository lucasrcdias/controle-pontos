# config/application.yml
defaults: &defaults
  app: "Imperium"
  host: "localhost:3000"
  company_url: "http://www.imperium.com.br"
  smtp:
    from: 'Imperium <noreply@zigotto.com.br>'
    settings:
      address: "smtp.gmail.com"
      port: 587
      domain: "zigotto.com.br"
      user_name: "noreply@zigotto.com.br"
      password: "zigmara8585"
      authentication: "plain"
      enable_starttls_auto: true

development:
  <<: *defaults

test:
  <<: *defaults

production:
  <<: *defaults
